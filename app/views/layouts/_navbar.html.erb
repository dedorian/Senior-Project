<nav class='navbar navbar-default' role='navigation'>
    <div class='container-fluid'>
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class='navbar-header'>
            <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='#bs-docs-example-navbar-collapse-1'>
                <span class='sr-only'>Toggle navigation</span>
                <span class='icon-bar'></span>
                <span class='icon-bar'></span>
                <span class='icon-bar'></span>
            </button>
            <div class='navbar-brand'>
                <%= link_to 'WSC', root_path %>
            </div>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div id='bs-docs-example-navbar-collapse-1'>
            <ul class='nav navbar-nav'>

                <!-- Catalog -->
                <li>
                    <%= link_to 'Catalog', products_path %>
                </li>

                <!-- Order -->
                <li>
                    <%= link_to 'Order', orders_path %>
                </li>

                <!-- Contact Us -->
                <li>
                    <%= link_to 'Contact Us', new_message_path %>
                </li>

                <!-- Customer Support -->
                <li>
                    <%= link_to 'Customer Support', support_path %>
                </li>

            </ul>

            <ul class='nav navbar-nav navbar-right'>
                <% if user_signed_in? %>
                <% if current_user.admin? %>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Admin Controlls
                    </a>
                    <ul class="dropdown-menu">
                        <li><%= link_to "Customer Messages", messages_path %></li>
                        <li><%= link_to "Orders", orders_path %></li>
                    </ul>
                </li>
                <% end %>
                <li><%= button_to "Account", user_registration_path, style: "margin-top:13px; margin-right:5px;"%></li>
                <li><%= button_to "Logout", destroy_user_session_path, :method => :delete, style: "margin-top:13px; margin-right:5px;" %></li>
            <% else %>
                <li><%= button_to "Register", new_user_registration_path, :method => :get, style: "margin-top:13px; margin-right:5px;"%></li>
                <li><%= button_to "Login", new_user_session_path, style: "margin-top:13px; margin-right:5px;"%></li>
                <% end %>
            </ul>

        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
